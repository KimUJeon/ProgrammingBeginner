def checkio(array):    # array 는 리스트를 의미한다.
    if not array:
        return 0

    return sum(array[::2]) * array[-1] # 인덱스 마지막값은 음수를 사용하면 뒤에서부터 센다.


if __name__ == '__main__':
    assert checkio([0, 1, 2, 3, 4, 5]) == 30, "(0+2+4)*5=30"
    assert checkio([1, 3, 5]) == 30, "(1+5)*5=30"
    assert checkio([6]) == 36, ""
    print("Coding Complete!")
    

You are given an array of integers. You should find the sum of the elements with even indexes(0th, 2nd, 4th...)
then multiply this summed number and the final element of the array together. Don't firget that the first element has
an index of 0.

For an empty array, the result will always be 0 (zero).

Input: A list of integers.

Output: The number as an interger.
