# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean

a = int(input())
b = float(input())
c = 2j
d = 0xDA

print(a + b, type(a + b))
print(c.real, c.imag)
print(c + d, type(c + d))


1. 정수형은 우리가 사용해온 일반적인 정수이며, 프로그램에서는 int형이라고 저장합니다.
2. 실수형은 소숫점이 포함된 숫자를 말하고 프로그램상에서는 float형 이라고 저장합니다.
3. 복소수형은 허수를 포함한 수를 의미합니다. 복소수의 실수부는 "변수.real", 허수부는 "변수.imag"로 따로 반환할 수 있습니다.
   그리고 "변수.conjgate()"는 켤레복소수를 반환합니다.
4. 8진수는 Octal의 O(o)를 따와서 0o155형태로 작성합니다.
4. 16진수는 Hex의 x를 따와서 0x1A 형태로 씁니다. 2진수는 1과 0으로 표현합니다.
   이 모든 수는 따로 형 변환을 하지 않고 연산이 가능하고 출력은 10진수로 됩니다.
