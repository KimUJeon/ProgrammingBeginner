for i in range(1, 10):
  for j in range(1, 10): 
    print("%d * %d = %d" %(i, j, i*j))


input() 은 입력받은 값을 문자열로 지정한다. 그래서 정수나 실수형으로 저장하고 싶은 경우
input() 앞에 int() 혹은 float()을 사용해야 한다.
"for 변수 in range():" 여기에 모든 조건부가 포함이 되어 있다.
예를 들어 for i in range(2, 12, 2): 는 for 문을 시작할때 2를 먼저 i 에 대입하고, 반복문의 진행이 끝나면
i에 대입될 값을 2씩 증가시키고 i에 12가 대입되기 전까지 반복한다.
